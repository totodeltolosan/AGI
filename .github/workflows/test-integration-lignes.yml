name: "Test d'Intégration : Division Loi Lignes"

# Test de bout-en-bout de la chaîne de production complète
on:
  workflow_dispatch:
    inputs:
      limite_lignes_test:
        description: "Limite de lignes pour le test (défaut: 200)"
        required: false
        default: "200"
        type: string
      fichiers_a_analyser:
        description: "Pattern de fichiers à analyser pour le test"
        required: false
        default: "*.py"
        type: string
      mode_debug:
        description: "Activer le mode debug détaillé"
        required: false
        default: false
        type: boolean

# Permissions nécessaires
permissions:
  contents: read
  issues: write
  checks: write

jobs:
  # ============================================================================
  # ÉTAPE 0 : Préparation des données de test
  # ============================================================================
  
  preparation_donnees:
    name: "Préparation des données de test"
    runs-on: ubuntu-latest
    outputs:
      artefact_liste_fichiers: ${{ steps.scan.outputs.artefact_path }}
      total_fichiers_detectes: ${{ steps.scan.outputs.fichiers_trouves }}
    
    steps:
      - name: "Checkout du code avec historique"
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: "Scan initial des fichiers à analyser"
        id: scan
        uses: ./.github/workflows/06-01_scanner-fichiers.yml
        with:
          pattern: ${{ inputs.fichiers_a_analyser }}
          chemin_racine: "./"
          exclusions: '["test_*", "*__pycache__*", "*.pyc", ".git/*"]'
          nom_artefact: "liste-fichiers-test-lignes.json"
      
      - name: "Validation de la préparation"
        run: |
          echo "🔍 Données de test préparées"
          echo "   • Pattern analysé : ${{ inputs.fichiers_a_analyser }}"
          echo "   • Fichiers détectés : ${{ steps.scan.outputs.fichiers_trouves }}"
          echo "   • Artefact : ${{ steps.scan.outputs.artefact_path }}"
          
          if [ "${{ steps.scan.outputs.fichiers_trouves }}" = "0" ]; then
            echo "⚠️  Aucun fichier détecté - le test continuera avec des données vides"
          fi

  # ============================================================================
  # ÉTAPE 1 : Compteur de Lignes + Validation
  # ============================================================================
  
  etape_compteur:
    name: "1️⃣ Compteur + Validation"
    runs-on: ubuntu-latest
    needs: preparation_donnees
    outputs:
      artefact_resultats_bruts: ${{ steps.compteur.outputs.artefact_resultats }}
      fichiers_analyses: ${{ steps.compteur.outputs.total_fichiers_analyses }}
      total_lignes: ${{ steps.compteur.outputs.total_lignes }}
      validation_compteur_ok: ${{ steps.compteur.outputs.validation_reussie }}
    
    steps:
      - name: "Checkout du code"
        uses: actions/checkout@v4
      
      - name: "Exécution Ouvrier Compteur"
        id: compteur
        uses: ./.github/workflows/04-01-lignes-compteur.yml
        with:
          artefact_liste_fichiers: ${{ needs.preparation_donnees.outputs.artefact_liste_fichiers }}
          inclure_vides: true
          inclure_commentaires: true
          nom_artefact_sortie: "resultats-bruts-compteur-test.json"
      
      - name: "Rapport Étape 1"
        run: |
          echo "✅ ÉTAPE 1 TERMINÉE - Compteur de Lignes"
          echo "   • Fichiers analysés : ${{ steps.compteur.outputs.total_fichiers_analyses }}"
          echo "   • Total lignes : ${{ steps.compteur.outputs.total_lignes }}"
          echo "   • Validation : $([ '${{ steps.compteur.outputs.validation_reussie }}' = 'true' ] && echo 'RÉUSSIE' || echo 'ÉCHOUÉE')"
          
          if [ "${{ steps.compteur.outputs.validation_reussie }}" != "true" ]; then
            echo "❌ ARRÊT: Validation du compteur échouée"
            exit 1
          fi

  # ============================================================================
  # ÉTAPE 2 : Juge de Conformité + Validation
  # ============================================================================
  
  etape_juge:
    name: "2️⃣ Juge + Validation"
    runs-on: ubuntu-latest
    needs: etape_compteur
    outputs:
      artefact_resultats_juges: ${{ steps.juge.outputs.artefact_resultats }}
      taux_conformite: ${{ steps.juge.outputs.taux_conformite }}
      verdict_global: ${{ steps.juge.outputs.verdict_global }}
      validation_juge_ok: ${{ steps.juge.outputs.validation_reussie }}
    
    steps:
      - name: "Checkout du code"
        uses: actions/checkout@v4
      
      - name: "Exécution Ouvrier Juge"
        id: juge
        uses: ./.github/workflows/04-02-lignes-juge.yml
        with:
          artefact_resultats_bruts: ${{ needs.etape_compteur.outputs.artefact_resultats_bruts }}
          limite_lignes: ${{ inputs.limite_lignes_test }}
          nom_artefact_sortie: "resultats-juges-test.json"
      
      - name: "Rapport Étape 2"
        run: |
          echo "✅ ÉTAPE 2 TERMINÉE - Juge de Conformité"
          echo "   • Limite appliquée : ${{ inputs.limite_lignes_test }} lignes"
          echo "   • Taux conformité : ${{ steps.juge.outputs.taux_conformite }}%"
          echo "   • Verdict : ${{ steps.juge.outputs.verdict_global }}"
          echo "   • Validation : $([ '${{ steps.juge.outputs.validation_reussie }}' = 'true' ] && echo 'RÉUSSIE' || echo 'ÉCHOUÉE')"
          
          if [ "${{ steps.juge.outputs.validation_reussie }}" != "true" ]; then
            echo "❌ ARRÊT: Validation du juge échouée"
            exit 1
          fi

  # ============================================================================
  # ÉTAPE 3 : Statisticien + Validation
  # ============================================================================
  
  etape_statisticien:
    name: "3️⃣ Statisticien + Validation"
    runs-on: ubuntu-latest
    needs: etape_juge
    outputs:
      artefact_statistiques: ${{ steps.stats.outputs.artefact_resultats }}
      moyenne_lignes: ${{ steps.stats.outputs.moyenne_lignes }}
      validation_stats_ok: ${{ steps.stats.outputs.validation_reussie }}
    
    steps:
      - name: "Checkout du code"
        uses: actions/checkout@v4
      
      - name: "Exécution Ouvrier Statisticien"
        id: stats
        uses: ./.github/workflows/04-03-lignes-statisticien.yml
        with:
          artefact_resultats_juges: ${{ needs.etape_juge.outputs.artefact_resultats_juges }}
          inclure_percentiles: true
          nom_artefact_sortie: "statistiques-test.json"
      
      - name: "Rapport Étape 3"
        run: |
          echo "✅ ÉTAPE 3 TERMINÉE - Statisticien"
          echo "   • Moyenne lignes/fichier : ${{ steps.stats.outputs.moyenne_lignes }}"
          echo "   • Taux conformité calculé : ${{ steps.stats.outputs.taux_conformite }}%"
          echo "   • Validation : $([ '${{ steps.stats.outputs.validation_reussie }}' = 'true' ] && echo 'RÉUSSIE' || echo 'ÉCHOUÉE')"
          
          if [ "${{ steps.stats.outputs.validation_reussie }}" != "true" ]; then
            echo "❌ ARRÊT: Validation du statisticien échouée"
            exit 1
          fi

  # ============================================================================
  # ÉTAPE 4 : Rapporteur + Validation
  # ============================================================================
  
  etape_rapporteur:
    name: "4️⃣ Rapporteur + Validation"
    runs-on: ubuntu-latest
    needs: [etape_juge, etape_statisticien]
    outputs:
      artefact_rapport: ${{ steps.rapport.outputs.artefact_resultats }}
      total_lignes_rapport: ${{ steps.rapport.outputs.total_lignes_rapport }}
      validation_rapport_ok: ${{ steps.rapport.outputs.validation_reussie }}
    
    steps:
      - name: "Checkout du code"
        uses: actions/checkout@v4
      
      - name: "Exécution Ouvrier Rapporteur"
        id: rapport
        uses: ./.github/workflows/04-04-lignes-rapporteur.yml
        with:
          artefact_resultats_juges: ${{ needs.etape_juge.outputs.artefact_resultats_juges }}
          artefact_statistiques: ${{ needs.etape_statisticien.outputs.artefact_statistiques }}
          format_detaille: true
          nom_artefact_sortie: "rapport-lignes-test.csv"
      
      - name: "Rapport Étape 4"
        run: |
          echo "✅ ÉTAPE 4 TERMINÉE - Rapporteur"
          echo "   • Lignes dans le rapport : ${{ steps.rapport.outputs.total_lignes_rapport }}"
          echo "   • Taux conformité rapport : ${{ steps.rapport.outputs.taux_conformite_rapport }}%"
          echo "   • Validation : $([ '${{ steps.rapport.outputs.validation_reussie }}' = 'true' ] && echo 'RÉUSSIE' || echo 'ÉCHOUÉE')"
          
          if [ "${{ steps.rapport.outputs.validation_reussie }}" != "true" ]; then
            echo "❌ ARRÊT: Validation du rapporteur échouée"
            exit 1
          fi

  # ============================================================================
  # ÉTAPE 5 : Conseiller + Validation
  # ============================================================================
  
  etape_conseiller:
    name: "5️⃣ Conseiller + Validation"
    runs-on: ubuntu-latest
    needs: etape_statisticien
    outputs:
      artefact_recommandations: ${{ steps.conseils.outputs.artefact_resultats }}
      taille_document: ${{ steps.conseils.outputs.taille_document }}
      validation_conseils_ok: ${{ steps.conseils.outputs.validation_reussie }}
    
    steps:
      - name: "Checkout du code"
        uses: actions/checkout@v4
      
      - name: "Exécution Ouvrier Conseiller"
        id: conseils
        uses: ./.github/workflows/04-05-lignes-conseiller.yml
        with:
          artefact_statistiques: ${{ needs.etape_statisticien.outputs.artefact_statistiques }}
          niveau_detail: "expert"
          inclure_exemples: true
          nom_artefact_sortie: "recommandations-test.md"
      
      - name: "Rapport Étape 5"
        run: |
          echo "✅ ÉTAPE 5 TERMINÉE - Conseiller"
          echo "   • Taille document : ${{ steps.conseils.outputs.taille_document }} caractères"
          echo "   • Validation : $([ '${{ steps.conseils.outputs.validation_reussie }}' = 'true' ] && echo 'RÉUSSIE' || echo 'ÉCHOUÉE')"
          
          if [ "${{ steps.conseils.outputs.validation_reussie }}" != "true" ]; then
            echo "❌ ARRÊT: Validation du conseiller échouée"
            exit 1
          fi

  # ============================================================================
  # RAPPORT FINAL D'INTÉGRATION
  # ============================================================================
  
  rapport_integration_final:
    name: "📊 Rapport Final d'Intégration"
    runs-on: ubuntu-latest
    needs: [preparation_donnees, etape_compteur, etape_juge, etape_statisticien, etape_rapporteur, etape_conseiller]
    if: always()
    
    steps:
      - name: "Checkout du code"
        uses: actions/checkout@v4
      
      - name: "Analyse des résultats de la chaîne complète"
        run: |
          echo "🔗 ANALYSE DE LA CHAÎNE DE PRODUCTION COMPLÈTE"
          echo "=============================================="
          
          # Vérification du statut de chaque étape
          etapes=(
            "preparation_donnees:${{ needs.preparation_donnees.result }}"
            "etape_compteur:${{ needs.etape_compteur.result }}"
            "etape_juge:${{ needs.etape_juge.result }}"
            "etape_statisticien:${{ needs.etape_statisticien.result }}"
            "etape_rapporteur:${{ needs.etape_rapporteur.result }}"
            "etape_conseiller:${{ needs.etape_conseiller.result }}"
          )
          
          echo ""
          echo "📋 STATUT DE CHAQUE ÉTAPE :"
          success_count=0
          total_etapes=6
          
          for etape in "${etapes[@]}"; do
            name="${etape%%:*}"
            status="${etape##*:}"
            case "$status" in
              "success")
                echo "   ✅ $name : RÉUSSI"
                ((success_count++))
                ;;
              "failure")
                echo "   ❌ $name : ÉCHOUÉ"
                ;;
              "cancelled"|"skipped")
                echo "   ⏭️  $name : IGNORÉ"
                ;;
              *)
                echo "   ❓ $name : STATUT INCONNU ($status)"
                ;;
            esac
          done
          
          echo ""
          echo "📊 MÉTRIQUES DE LA CHAÎNE :"
          echo "   • Étapes réussies : $success_count/$total_etapes"
          echo "   • Fichiers détectés : ${{ needs.preparation_donnees.outputs.total_fichiers_detectes }}"
          echo "   • Fichiers analysés : ${{ needs.etape_compteur.outputs.fichiers_analyses }}"
          echo "   • Total lignes : ${{ needs.etape_compteur.outputs.total_lignes }}"
          echo "   • Limite appliquée : ${{ inputs.limite_lignes_test }} lignes"
          echo "   • Taux conformité : ${{ needs.etape_juge.outputs.taux_conformite }}%"
          echo "   • Verdict global : ${{ needs.etape_juge.outputs.verdict_global }}"
          echo "   • Moyenne lignes/fichier : ${{ needs.etape_statisticien.outputs.moyenne_lignes }}"
          
          echo ""
          echo "📄 ARTEFACTS GÉNÉRÉS :"
          artefacts=(
            "Liste fichiers:${{ needs.preparation_donnees.outputs.artefact_liste_fichiers }}"
            "Résultats bruts:${{ needs.etape_compteur.outputs.artefact_resultats_bruts }}"
            "Jugements:${{ needs.etape_juge.outputs.artefact_resultats_juges }}"
            "Statistiques:${{ needs.etape_statisticien.outputs.artefact_statistiques }}"
            "Rapport CSV:${{ needs.etape_rapporteur.outputs.artefact_rapport }}"
            "Recommandations:${{ needs.etape_conseiller.outputs.artefact_recommandations }}"
          )
          
          for artefact in "${artefacts[@]}"; do
            name="${artefact%%:*}"
            file="${artefact##*:}"
            echo "   📎 $name : $file"
          done
          
          echo ""
          echo "🔍 VALIDATIONS PAR QUALITICIENS :"
          validations=(
            "Compteur:${{ needs.etape_compteur.outputs.validation_compteur_ok }}"
            "Juge:${{ needs.etape_juge.outputs.validation_juge_ok }}"
            "Statisticien:${{ needs.etape_statisticien.outputs.validation_stats_ok }}"
            "Rapporteur:${{ needs.etape_rapporteur.outputs.validation_rapport_ok }}"
            "Conseiller:${{ needs.etape_conseiller.outputs.validation_conseils_ok }}"
          )
          
          validations_ok=0
          for validation in "${validations[@]}"; do
            name="${validation%%:*}"
            status="${validation##*:}"
            if [ "$status" = "true" ]; then
              echo "   ✅ $name : VALIDÉ"
              ((validations_ok++))
            else
              echo "   ❌ $name : VALIDATION ÉCHOUÉE"
            fi
          done
          
          echo ""
          echo "📈 RÉSUMÉ GLOBAL :"
          success_rate=$((success_count * 100 / total_etapes))
          validation_rate=$((validations_ok * 100 / 5))  # 5 validations attendues
          
          echo "   • Taux succès étapes : ${success_rate}%"
          echo "   • Taux succès validations : ${validation_rate}%"
          
          if [ $success_count -eq $total_etapes ] && [ $validations_ok -eq 5 ]; then
            echo ""
            echo "🏆 INTÉGRATION COMPLÈTEMENT RÉUSSIE !"
            echo "✅ La chaîne de production Division 'Loi Lignes' fonctionne parfaitement"
            echo "🚀 Prêt pour l'intégration avec le Contremaître (Niveau 3)"
          elif [ $success_count -ge 4 ] && [ $validations_ok -ge 3 ]; then
            echo ""
            echo "👍 INTÉGRATION LARGEMENT RÉUSSIE"
            echo "⚠️  Quelques problèmes mineurs à corriger"
          else
            echo ""
            echo "🚨 INTÉGRATION PARTIELLEMENT ÉCHOUÉE"
            echo "🔧 Corrections nécessaires avant passage au Contremaître"
          fi

      - name: "Création du check de statut final"
        uses: ./.github/workflows/07-03_formateur-statut.yml
        with:
          resultat: ${{ needs.preparation_donnees.result == 'success' && needs.etape_compteur.result == 'success' && needs.etape_juge.result == 'success' && needs.etape_statisticien.result == 'success' && needs.etape_rapporteur.result == 'success' && needs.etape_conseiller.result == 'success' }}
          message_succes: "✅ Test d'intégration Division Loi Lignes : CHAÎNE COMPLÈTE RÉUSSIE"
          message_echec: "❌ Test d'intégration Division Loi Lignes : ÉCHECS DANS LA CHAÎNE"
          nom_check: "AGI-Gouvernance/Integration-Loi-Lignes"

      - name: "Résumé dans l'interface GitHub Actions"
        if: always()
        run: |
          echo "## 🔗 Test d'Intégration - Division Loi Lignes" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Pattern analysé :** \`${{ inputs.fichiers_a_analyser }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Limite de test :** ${{ inputs.limite_lignes_test }} lignes/fichier" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          # Statut global
          if [ "${{ needs.preparation_donnees.result }}" = "success" ] && \
             [ "${{ needs.etape_compteur.result }}" = "success" ] && \
             [ "${{ needs.etape_juge.result }}" = "success" ] && \
             [ "${{ needs.etape_statisticien.result }}" = "success" ] && \
             [ "${{ needs.etape_rapporteur.result }}" = "success" ] && \
             [ "${{ needs.etape_conseiller.result }}" = "success" ]; then
            echo "**🏆 RÉSULTAT GLOBAL : SUCCÈS COMPLET**" >> $GITHUB_STEP_SUMMARY
          else
            echo "**🚨 RÉSULTAT GLOBAL : ÉCHECS DÉTECTÉS**" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Métriques de la chaîne" >> $GITHUB_STEP_SUMMARY
          echo "- **Fichiers analysés :** ${{ needs.etape_compteur.outputs.fichiers_analyses }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Total lignes :** ${{ needs.etape_compteur.outputs.total_lignes }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Taux conformité :** ${{ needs.etape_juge.outputs.taux_conformite }}%" >> $GITHUB_STEP_SUMMARY
          echo "- **Verdict :** ${{ needs.etape_juge.outputs.verdict_global }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "*Test d'intégration terminé le $(date '+%Y-%m-%d %H:%M:%S')*" >> $GITHUB_STEP_SUMMARY

  # ============================================================================
  # DEBUG (si activé)
  # ============================================================================
  
  debug_integration:
    name: "🔍 Debug Intégration"
    runs-on: ubuntu-latest
    needs: [preparation_donnees, etape_compteur, etape_juge, etape_statisticien, etape_rapporteur, etape_conseiller]
    if: inputs.mode_debug && always()
    
    steps:
      - name: "Informations debug détaillées"
        run: |
          echo "🔍 DEBUG - INFORMATIONS DÉTAILLÉES D'INTÉGRATION"
          echo "=============================================="
          echo ""
          echo "Variables d'entrée :"
          echo "  • limite_lignes_test: ${{ inputs.limite_lignes_test }}"
          echo "  • fichiers_a_analyser: ${{ inputs.fichiers_a_analyser }}"
          echo "  • mode_debug: ${{ inputs.mode_debug }}"
          echo ""
          echo "Statuts des jobs :"
          echo "  • preparation_donnees: ${{ needs.preparation_donnees.result }}"
          echo "  • etape_compteur: ${{ needs.etape_compteur.result }}"
          echo "  • etape_juge: ${{ needs.etape_juge.result }}"
          echo "  • etape_statisticien: ${{ needs.etape_statisticien.result }}"
          echo "  • etape_rapporteur: ${{ needs.etape_rapporteur.result }}"
          echo "  • etape_conseiller: ${{ needs.etape_conseiller.result }}"
          echo ""
          echo "Outputs de chaque étape :"
          echo "  • Fichiers détectés: ${{ needs.preparation_donnees.outputs.total_fichiers_detectes }}"
          echo "  • Artefact liste: ${{ needs.preparation_donnees.outputs.artefact_liste_fichiers }}"
          echo "  • Fichiers analysés: ${{ needs.etape_compteur.outputs.fichiers_analyses }}"
          echo "  • Total lignes: ${{ needs.etape_compteur.outputs.total_lignes }}"
          echo "  • Taux conformité: ${{ needs.etape_juge.outputs.taux_conformite }}"
          echo "  • Verdict: ${{ needs.etape_juge.outputs.verdict_global }}"
          echo "  • Moyenne lignes: ${{ needs.etape_statisticien.outputs.moyenne_lignes }}"
